<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<!-- dev.mvc.cate.CateDAOInter 패키지에 등록된 interface 명시,
      패키지명과 인터페이스명은 실제로 존재해야함,
      Spring이 내부적으로 자동으로 interface를 구현해줌. -->
<mapper namespace="dev.mvc.cart.CartDAOInter">
  <insert id="create" parameterType="CartVO">
    INSERT INTO cart(cart_no, product_no, cart_cnt, cart_tot, cart_rdate)
    VALUES(cart_seq.nextval, #{product_no}, #{cart_cnt}, #{cart_tot}, sysdate)
  </insert> 
  
  <select id= "list" resultType= "CartVO">
    SELECT cart_no, product_no, cart_cnt, cart_tot, cart_rdate
    FROM cart
    ORDER BY cart_no ASC
  </select>
  
  <select id="read" resultType="ProductVO" parameterType="int">
    SELECT cart_no, product_no, cart_cnt, cart_tot, cart_rdate
    FROM cart
    WHERE cart_no = #{cart_no}
  </select>
  <update id="update" parameterType="ProductVO">
    UPDATE cart
    SET cart_cnt=#{cart_cnt},cart_tot=#{cart_tot}
    WHERE cart_no = #{cart_no}
  </update>
  
  <delete id="delete" parameterType="int">
    DELETE FROM cart
    WHERE cart_no=#{cart_no}
  </delete>
  
  
          
</mapper>